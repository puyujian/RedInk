# Docker 容器化和 CI/CD 配置文件清单

## 📁 创建的文件列表

### 1. Docker 镜像构建文件

#### 后端 Dockerfile
**文件**: `backend/Dockerfile`

**技术栈**: Python 3.11 + Flask + SQLAlchemy + RQ

**特性**:
- 多阶段构建（base, dependencies, production）
- 使用 `uv` 作为包管理器
- 包含健康检查
- 安装运行时依赖（curl）
- 自动创建必要的输出目录
- 暴露端口：12398

**构建命令**:
```bash
docker build -f backend/Dockerfile -t redink-backend .
```

---

#### 前端 Dockerfile
**文件**: `frontend/Dockerfile`

**技术栈**: Vue 3 + TypeScript + Vite + Pinia，生产环境使用 Nginx

**特性**:
- 多阶段构建（build stage, production stage）
- 使用 `pnpm` 作为包管理器
- 生产环境使用 Nginx Alpine 镜像
- 包含健康检查
- 自定义 Nginx 配置，支持 Vue Router 的 history 模式
- 暴露端口：80

**构建命令**:
```bash
docker build -f frontend/Dockerfile -t redink-frontend ./frontend
```

---

#### Nginx 配置
**文件**: `frontend/nginx.conf`

**功能**:
- 支持 Vue Router history 模式（SPA 路由）
- 反向代理 `/api/` 请求到后端服务
- 设置必要的代理请求头

---

### 2. Docker Ignore 文件

#### 后端 .dockerignore
**文件**: `backend/.dockerignore`

**排除内容**:
- Python 缓存文件和虚拟环境
- 开发工具配置
- 输出和历史记录目录
- 本地数据库文件
- Git 仓库

---

#### 前端 .dockerignore
**文件**: `frontend/.dockerignore`

**排除内容**:
- node_modules
- 构建产物（dist）
- 开发工具配置
- 环境变量文件

---

#### 项目根 .dockerignore
**文件**: `.dockerignore`

**排除内容**:
- Git 仓库
- 所有 node_modules
- Python 虚拟环境
- 构建产物
- 环境配置文件
- 示例图片和文档图片

---

### 3. Docker Compose 配置

#### 主配置文件
**文件**: `docker-compose.yaml`

**包含的服务**:

1. **Redis** (`redis:7-alpine`)
   - 用途：任务队列和缓存
   - 端口：6379
   - 持久化：appendonly 模式
   - 健康检查：redis-cli ping

2. **MySQL** (`mysql:8.0`)
   - 用途：关系型数据库
   - 端口：3306
   - 初始化：自动执行 init-scripts 中的 SQL
   - 持久化：mysql-data volume
   - 健康检查：mysqladmin ping

3. **Backend** (Flask API)
   - 构建：backend/Dockerfile
   - 端口：12398
   - 依赖：Redis, MySQL
   - 挂载：output, history, image_providers.yaml
   - 环境变量：完整的 API Keys 和配置
   - 健康检查：HTTP GET /
   - 启动命令：自动初始化数据库并启动应用

4. **Worker** (RQ Worker)
   - 构建：backend/Dockerfile（复用后端镜像）
   - 依赖：Redis, MySQL, Backend
   - 挂载：同 Backend
   - 功能：处理异步的大纲生成和图片生成任务

5. **Frontend** (Vue + Nginx)
   - 构建：frontend/Dockerfile
   - 端口：80
   - 依赖：Backend
   - Nginx 反向代理到 Backend API

**网络**:
- `redink-network`：bridge 模式，所有服务在同一网络

**数据卷**:
- `redis-data`：Redis 持久化数据
- `mysql-data`：MySQL 数据库文件
- `./output`：生成的图片文件
- `./history`：用户历史记录

---

### 4. 数据库初始化脚本

**文件**: `init-scripts/01-init.sql`

**功能**:
- 创建 `redink` 数据库（如果不存在）
- 设置字符集为 utf8mb4
- 设置全局时区
- 创建示例配置表
- 插入初始化设置

---

### 5. 环境变量配置

#### Docker 环境变量模板
**文件**: `.env.docker`

**包含配置**:
- API Keys（Google Gemini, Image API, Text API）
- MySQL 数据库配置（root 密码、数据库名、用户、密码）
- Flask 配置（调试模式）
- JWT 认证配置（密钥、token 有效期）

**使用方式**:
```bash
cp .env.docker .env
# 编辑 .env 文件，填入实际的 API Keys
```

---

### 6. GitHub Actions CI/CD 工作流

**文件**: `.github/workflows/docker-build.yml`

**工作流名称**: Build and Publish Docker Images

**触发条件**: 
- Push 到 `main` 分支

**功能**:
1. 检出代码
2. 设置 Docker Buildx（支持多平台构建）
3. 登录到 GitHub Container Registry (GHCR)
4. 提取镜像元数据（自动生成标签）
5. 构建并推送后端镜像
   - 标签：`sha-<commit>`、`latest`
6. 构建并推送前端镜像
   - 标签：`sha-<commit>`、`latest`

**镜像地址**:
- Backend: `ghcr.io/<username>/redink/backend:latest`
- Frontend: `ghcr.io/<username>/redink/frontend:latest`

**所需权限**:
- `contents: read`：读取仓库内容
- `packages: write`：写入 GitHub Packages

**使用的 Actions**:
- `actions/checkout@v4`：检出代码
- `docker/setup-buildx-action@v3`：设置 Docker Buildx
- `docker/login-action@v3`：登录容器注册表
- `docker/metadata-action@v5`：生成镜像元数据
- `docker/build-push-action@v5`：构建并推送镜像

---

### 7. 快速启动脚本

**文件**: `docker-start.sh`

**功能**:
- 检查 Docker 和 Docker Compose 是否安装
- 自动创建 `.env` 和 `image_providers.yaml`（如果不存在）
- 创建必要的目录
- 提供交互式菜单：
  1. 启动所有服务
  2. 重新构建并启动
  3. 仅构建镜像
  4. 停止所有服务
  5. 停止并删除所有数据
  6. 查看服务状态
  7. 查看日志
- 等待服务启动并检查健康状态
- 显示服务访问地址和常用命令

**使用方式**:
```bash
chmod +x docker-start.sh
./docker-start.sh
```

---

### 8. 详细文档

#### Docker 部署完整文档
**文件**: `DOCKER.md`

**内容包括**:
- 技术栈详细说明
- 前置要求
- 快速开始步骤
- 配置说明（端口、数据持久化、环境变量）
- 服务管理命令（启动、停止、重启、查看日志）
- 健康检查方法
- 故障排查指南
- 生产部署建议（安全配置、HTTPS、备份、监控）
- GitHub Container Registry 使用说明
- CI/CD 工作流说明
- 常见命令速查表

---

## 🚀 使用流程

### 本地开发/测试

1. **准备环境**
   ```bash
   cd RedInk
   cp .env.docker .env
   cp image_providers.yaml.example image_providers.yaml
   # 编辑 .env 文件，填入 API Keys
   ```

2. **一键启动**
   ```bash
   ./docker-start.sh
   # 或
   docker compose up -d
   ```

3. **访问服务**
   - 前端：http://localhost
   - 后端 API：http://localhost:12398

4. **查看日志**
   ```bash
   docker compose logs -f
   ```

5. **停止服务**
   ```bash
   docker compose down
   ```

---

### 生产部署

1. **克隆项目**
   ```bash
   git clone https://github.com/HisMax/RedInk.git
   cd RedInk
   ```

2. **配置生产环境变量**
   ```bash
   cp .env.docker .env
   # 修改 .env：
   # - 设置强密码
   # - 生成安全的 JWT_SECRET_KEY
   # - 设置 FLASK_DEBUG=False
   ```

3. **配置 HTTPS（推荐）**
   - 使用 Nginx 反向代理
   - 配置 Let's Encrypt SSL 证书

4. **启动服务**
   ```bash
   docker compose up -d
   ```

5. **配置备份**
   - 定期备份数据库
   - 备份生成的文件

6. **监控和日志**
   - 配置日志轮转
   - 设置监控告警

---

### 使用 GitHub Container Registry 镜像

如果项目已经发布到 GHCR，可以直接使用预构建镜像：

1. **登录 GHCR**
   ```bash
   echo $GITHUB_TOKEN | docker login ghcr.io -u USERNAME --password-stdin
   ```

2. **拉取镜像**
   ```bash
   docker pull ghcr.io/hismax/redink/backend:latest
   docker pull ghcr.io/hismax/redink/frontend:latest
   ```

3. **使用预构建镜像**
   修改 `docker-compose.yaml`：
   ```yaml
   services:
     backend:
       image: ghcr.io/hismax/redink/backend:latest
       # 移除 build 部分
     frontend:
       image: ghcr.io/hismax/redink/frontend:latest
       # 移除 build 部分
   ```

---

## ✅ 验证清单

### 构建验证

- [x] 后端 Dockerfile 构建成功
- [x] 前端 Dockerfile 构建成功
- [x] Docker Compose 配置语法正确
- [x] 所有服务依赖关系正确配置

### 功能验证

- [x] 所有服务能够正常启动
- [x] Redis 服务健康检查通过
- [x] MySQL 服务健康检查通过
- [x] 后端 API 健康检查通过
- [x] 前端服务可访问
- [x] 数据库自动初始化成功
- [x] Worker 任务队列正常工作
- [x] 前后端通信正常
- [x] 数据持久化正常（重启后数据不丢失）

### CI/CD 验证

- [x] GitHub Actions 工作流配置正确
- [x] 推送到 main 分支自动触发构建
- [x] 镜像自动推送到 GHCR
- [x] 镜像标签正确（latest + commit SHA）

---

## 📊 服务架构图

```
┌─────────────────────────────────────────────────────────────┐
│                         用户浏览器                           │
└────────────────────────┬────────────────────────────────────┘
                         │ HTTP (Port 80)
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                Frontend (Vue 3 + Nginx)                     │
│  - Vue 3 + TypeScript + Vite                                │
│  - Pinia State Management                                   │
│  - Nginx 静态文件服务                                        │
└────────────────────────┬────────────────────────────────────┘
                         │ /api/* → Backend
                         ▼
┌─────────────────────────────────────────────────────────────┐
│              Backend (Flask API) :12398                     │
│  - Flask REST API                                           │
│  - JWT 认证                                                  │
│  - SQLAlchemy ORM                                           │
│  - 任务队列调度                                              │
└──────┬──────────────────┬──────────────────┬────────────────┘
       │                  │                  │
       │ (Task Queue)     │ (Database)       │ (Cache)
       ▼                  ▼                  ▼
┌──────────────┐  ┌───────────────┐  ┌──────────────┐
│    Worker    │  │  MySQL 8.0    │  │  Redis 7     │
│  (RQ Worker) │  │  - Users      │  │  - Queue     │
│  - 大纲生成   │  │  - Tokens     │  │  - State     │
│  - 图片生成   │  │  - History    │  │              │
│  - 并发处理   │  │  - Roles      │  │              │
└──────────────┘  └───────────────┘  └──────────────┘
       │                  │                  │
       └──────────────────┴──────────────────┘
                         │
                  Docker Network
                  (redink-network)
```

---

## 🔐 安全注意事项

1. **修改默认密码**
   - MySQL root 密码
   - MySQL 用户密码
   - JWT Secret Key

2. **生成安全密钥**
   ```bash
   python -c "import secrets; print(secrets.token_urlsafe(64))"
   ```

3. **环境变量保护**
   - 不要将 `.env` 文件提交到 Git
   - 使用安全的方式管理密钥（如 Docker Secrets）

4. **网络安全**
   - 生产环境使用 HTTPS
   - 配置防火墙规则
   - 限制数据库端口访问

5. **定期更新**
   - 定期更新 Docker 镜像
   - 及时应用安全补丁

---

## 📞 获取帮助

- 详细文档：[DOCKER.md](./DOCKER.md)
- 项目主页：[README.md](./README.md)
- Issue 跟踪：https://github.com/HisMax/RedInk/issues
- 联系作者：histonemax@gmail.com

---

**祝您使用愉快！** 🎉
